name: gec_student_faculty_hub
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  
  # Supabase for backend
  supabase_flutter: ^2.5.0  # Updated to latest stable
  
  # Charts and data visualization
  fl_chart: ^0.68.0  # Updated version
  
  # Date formatting
  intl: ^0.19.0  # Updated version
  
  # Additional useful packages
  provider: ^6.1.1  # For state management
  shared_preferences: ^2.2.2  # For local storage
  cached_network_image: ^3.3.1  # For image caching

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
  
  